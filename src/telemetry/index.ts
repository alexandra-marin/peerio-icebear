const S = require('./strings');
const { init, send } = require('./main');
const { textInput, duration } = require('./helpers');

module.exports = {
    duration,
    init,
    S,
    send,
    textInput
};
